version: "3"

services:
  postgres:
      container_name: tc-auth-service-tests-db
      image: postgres:10-alpine
      environment:
        POSTGRES_USERNAME: postgres
        POSTGRES_PASSWORD: admin123curl
      networks:
        outside_bridge:
          ipv4_address: 10.5.0.5

networks:
  outside_bridge:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 10.5.0.0/16